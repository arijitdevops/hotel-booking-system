# Base URL the SPA calls. Leave as /api to use the Vite dev proxy defined in
# vite.config.ts; point it at the deployed API origin in production, e.g.
# VITE_API_BASE_URL=https://api.example.com/api
VITE_API_BASE_URL=/api
